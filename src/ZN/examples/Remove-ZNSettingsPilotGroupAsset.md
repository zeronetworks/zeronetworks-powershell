### Example 1: Remove an asset from pilot group
```powershell
Remove-ZNSettingsPilotGroupAsset -OSType windows -Product cloud-connector -AssetIds @("a:a:o8w4Bpq7")
```

```output

```

This cmdlet removes assets from a pilot group.
